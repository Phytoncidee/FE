<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HikingLog_FE" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/green</item>
        <item name="colorOnPrimary">@color/green</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="android:displayOptions">useLogo|showHome|showTitle</item>
        <item name="android:logo">@drawable/logo</item> <!-- 로고 이미지 설정 -->
        <item name="android:textColorPrimary">@color/green</item> <!-- 앱 이름 색상 설정 -->
    </style>

    <style name="Theme.HikingLog_FE" parent="Base.Theme.HikingLog_FE" />
</resources>